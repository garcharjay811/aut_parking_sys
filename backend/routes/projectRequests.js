// const express = require("express");

// const ProjectRequestController = require("../controllers/projectRequests");

// const checkAuth = require("../middleware/check-auth");
// const extractFile = require("../middleware/file");

// const router = express.Router();
// // checkAuth, 

// router.get("/byUserId", checkAuth, ProjectRequestController.getProjectRequestByUserId);

// router.post("", extractFile, checkAuth, ProjectRequestController.createProjectRequest);

// router.put("/:id", extractFile, checkAuth, ProjectRequestController.updateProjectRequest);

// router.get("", ProjectRequestController.getProjectRequests);

// router.get("/:id", ProjectRequestController.getProjectRequest);


// // router.delete("/:id", checkAuth, ProjectRequestController.deleteProjectRequest);
// // getProjectRequestByUserId
// module.exports = router;
